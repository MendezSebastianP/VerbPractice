#set .venv
python -m venv .venv
source .venv/bin/activate

# requeriments
pip install -r requirements.txt


# password
Passw1234

# migrate (fill database)
python manage.py migrate 

# create users
python manage.py setup_test_users

#add your radis server port (it will run in the background until close). Already setup in 42, probably we need to type this:
docker run --rm -p -d 6379:6379 redis:7

# add your environement keys
echo "OPENAI_API_KEY='sk-...'" > myproject/.env