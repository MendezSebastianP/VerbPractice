#set .venv
python -m venv .venv
source .venv/bin/activate

# requeriments
pip install -r requirements.txt


# password
Passw1234

# migrate (fill database)
python manage.py migrate 

# create users
python manage.py setup_test_users

#(testing) add your radis server port. Already setup in 42, probably we need to type this:
docker run --rm -p 6379:6379 redis:7

# add your environement keys
echo "OPENAI_API_KEY='sk-...'" > myproject/.env